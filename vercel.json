{
  "framework": "nextjs",
  "buildCommand": "npx prisma db push && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron/check",
      "schedule": "0 8 * * *"
    }
  ]
}
